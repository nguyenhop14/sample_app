<li>
  <%= gravatar_for user, size: Settings.user.gravatar_forU %>
  <%= link_to user.name, user %>
    <% if current_user.admin? && !current_user? user %>
    | <%= link_to t("user._user.delete"), user, method: :delete, data: { confirm: t("user._user.sure") } %>
  <% end %>
</li>
